# Модуль diagnostics

Отвечает за страницу `/admin.php?tab=diagnostics`.

Проверяет:

- версию PHP;
- нужные PHP-расширения;
- наличие ключевых таблиц;
- наличие ключевых файлов;
- наличие важных колонок в `oca_results`.

Модуль ничего не изменяет в базе и не выполняет POST-действия.
